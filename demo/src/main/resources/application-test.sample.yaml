spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/demo?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.zhaojj11.template: debug
